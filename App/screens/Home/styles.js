import { StyleSheet } from "react-native";

//! BUTTONS STYLE SHEET

const styles = StyleSheet.create({
  btn: {
    height: 900,
    borderWidth: 5,
    shadowOffset: {
      width: 10,
      height: -10,
    },
    shadowColor: "black",
    marginTop: "50px",
    borderColor: "blue",
  },
});
